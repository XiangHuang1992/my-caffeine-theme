<nav class="navigation">
    <ul class="links">
        {{#foreach navigation}}
        <li class="nav-{{slug}} {{#if current}} nav-current{{/if}}">
            <a href="{{this.url}}">{{label}}</a>
        </li>
        {{/foreach}}
         <li class="nav-blog {{#if current}} nav-current{{/if}}">
            <a class="home-link" href="{{@blog.url}}/#open" title="{{@blog.title}} blog">博客</a>
        </li>
        <li class="subscribe-button nav-blog hide {{#if current}} nav-current{{/if}}">
            <a class="subscribe-link" href="{{@blog.url}}/#open" title="subscribe">RSS订阅</a>
        </li>
    </ul>
</nav>
